{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "DEBUG - Compilar para BIN",
            "command": "cmd",
            "args": [
                "/c",
                "echo === DIAGNÓSTICO === && echo Pasta atual: && cd && echo. && echo Pasta do workspace: && echo ${workspaceFolder} && echo. && echo Arquivo fonte: && echo ${file} && echo. && echo Destino: && echo ${workspaceFolder}\\bin\\${fileBasenameNoExtension}.exe && echo. && echo === COMPILANDO === && C:\\msys64\\ucrt64\\bin\\gcc.exe \"${file}\" -o \"${workspaceFolder}\\bin\\${fileBasenameNoExtension}.exe\" && echo. && echo === VERIFICANDO === && dir \"${workspaceFolder}\\bin\\\" && echo === FIM ==="
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Versão de diagnóstico que mostra tudo"
        }
    ]
}